import React from 'react';

const CategorySection = () => {
    return (
        <div>
            <h2 className="text-3xl font-bold text-center mb-8 text-primary">Select a category</h2>
        </div>
    );
};

export default CategorySection;